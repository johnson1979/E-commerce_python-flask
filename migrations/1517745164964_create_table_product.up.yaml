- args:
    sql: CREATE TABLE "product"("id" serial NOT NULL, "name" text NOT NULL, "price"
      text NOT NULL, "discount" text NOT NULL, "owner" text NOT NULL, PRIMARY KEY
      ("id") )
  type: run_sql
- args:
    name: product
  type: add_existing_table_or_view
